Implement the confirmed in-app feedback flow. The settings window's overflow menu gets a final separated `反馈` entry that opens a reusable right-side DrawerHost control. The drawer has no subtitle, supports Bug and Feature/experience-improvement structured forms matching the repository templates, keeps drafts on `暂时关闭`, clears them with the lower-right `取消反馈`, and contains a lower-left icon-and-text GitHub button opening the project Issues page. Do not add screenshot support.

Bug submissions must automatically attach the exact standard diagnostic ZIP generated by `IImportExportService.ExportDiagnosticAsync(..., includeExtendedData: false)`, with temporary-file cleanup after bounded send/flush. Feature submissions have no diagnostic attachment. Manual feedback is explicit and must work when `SentryTelemetryEnabled` is disabled without enabling background telemetry or persisting a setting change; use a short-lived feedback-only Sentry client behind an app-layer service. Keep all Sentry calls out of views, add focused unit tests for payload/attachment and failure cleanup, and use local assets for the GitHub brand mark.

CrashRecoveryWindow must replace its direct feedback button with a dropdown/menu: application feedback first, GitHub second. Ensure the startup crash prompt branch builds the Host before showing the recovery window (still before single-instance acquisition and without normal hosted services) so the common feedback and diagnostic services are available. The app feedback action can submit a crash Bug with current crash report context. Localize all new UI and status strings in SettingsView and CrashRecovery resource sets, register the resource designer/base resx according to project conventions, and update AGENTS if service/view/asset structure creates a durable convention.

Validate with focused tests, LSP/lens diagnostics, and the Release solution build/test commands. Do not disturb unrelated dirty `.pi/` or `.taskman/` files.